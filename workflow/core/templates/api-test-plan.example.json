{
  "schema_version": "1.0",
  "environment": {
    "name": "test",
    "base_url_env": "TEST_BASE_URL"
  },
  "cases": [
    {
      "id": "API-001",
      "method": "GET",
      "path": "/health",
      "headers": {
        "Authorization": "Bearer ${TEST_API_TOKEN}"
      },
      "expect": {
        "status": 200,
        "json": {
          "status": "ok"
        }
      }
    }
  ]
}
